# Set this in the Render Static Site dashboard, then trigger a new deploy.
# Use your deployed Backend Web Service URL, including /api; do not use localhost.
VITE_API_BASE_URL=https://your-backend-service.onrender.com/api
